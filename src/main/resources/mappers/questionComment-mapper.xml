<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edumate.boot.domain.teacher.model.mapper.QuestionCommentMapper" >
	<select id="selectQcList" resultType="QuestionComment">
		SELECT * FROM QUESTION_COMMENT WHERE QUESTION_NO = #{questionNo} AND QUESTION_COMMENT_YN = 'Y' ORDER BY QUESTION_COMMENT_NO DESC
	</select>
	<insert id="insertQuestionCommnet">
		INSERT INTO QUESTION_COMMENT (
			QUESTION_COMMENT_NO, QUESTION_NO, MEMBER_ID, QUESTION_COMMENT_CONTENT)
		VALUES (
			SEQ_QUESTION_COMMENT_NO.NEXTVAL, #{questionNo},
			#{memberId},
			#{questionCommentContent, jdbcType=VARCHAR} )
	</insert>
	<update id="deleteComment">
		UPDATE QUESTION_COMMENT 
		SET 
		    QUESTION_COMMENT_YN = 'N' 
		WHERE 
		    QUESTION_COMMENT_NO = #{questionCommentNo}
	</update>
</mapper>